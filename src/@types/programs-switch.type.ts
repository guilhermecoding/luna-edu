/**
 * Informações dos programas disponíveis para o switcher no layout
 * @property {string} name - Nome do programa.
 * @property {string} corporation - Nome da corporação associada ao programa.
 */
export type ProgramSwitch = {
    name: string;
}